DETECTOR_API_URL=https://tu-detector.com/api
DETECTOR_API_KEY=tu_token
# Opcional: umbral de porcentaje para considerar que es IA (por defecto 5)
AI_PERCENTAGE_THRESHOLD=5
# Opcional: puerto de ejecución (por defecto 3000)
PORT=3000
